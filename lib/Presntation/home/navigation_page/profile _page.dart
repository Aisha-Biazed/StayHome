import 'package:flutter/material.dart';
class ProfilePageNav extends StatelessWidget {
  const ProfilePageNav({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Profile Page"),
    );
  }
}
